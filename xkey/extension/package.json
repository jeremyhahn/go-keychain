{
  "name": "xkey-extension",
  "version": "1.0.0",
  "description": "xKey AutoFill browser extension",
  "private": true,
  "scripts": {
    "build": "node node_modules/typescript/lib/tsc.js && cp src/popup.html dist/ && cp manifest.json dist/ && mkdir -p dist/icons && (cp -r src/icons/* dist/icons/ 2>/dev/null || true)",
    "watch": "node node_modules/typescript/lib/tsc.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/chrome": "^0.0.287"
  }
}
