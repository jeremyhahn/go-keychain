services:
  test:
    build:
      context: ../../..
      dockerfile: test/integration/pkcs8/Dockerfile
    volumes:
      - ../../..:/app
      # Sibling module mounts (for go.mod replace directives)
      - ../../../../go-qrdb:/go-qrdb:cached
      - ../../../../go-quicraft:/go-quicraft:cached
    working_dir: /app

networks:
  default:
    name: xkms-pkcs8-test
